apiVersion: v1
kind: Service
metadata:
  name: pi-events
  namespace: pi-soyosa
spec:
  selector:
    app: pi-events
  ports:
    - protocol: TCP
      port: 8086
      targetPort: 8086
  type: ClusterIP
