apiVersion: v1
kind: Service
metadata:
  name: game-service
  namespace: pi-soyosa
spec:
  selector:
    app: gameservice
  ports:
    - protocol: TCP
      port: 8084
      targetPort: 8084
  type: ClusterIP
