package com.example.messageservice.repostories;
/*

import com.example.messageservice.dto.UserDTO;
import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;


@FeignClient(
        name = "userClient",
        url = "http://localhost:8070",
        path = "/profiles"
)
public interface UserClient {
    @GetMapping("/{id}")
    UserDTO getProfileById(@PathVariable("id") Long profileId);
    @GetMapping("/by-username/{username}")
    UserDTO getProfileByUsername(@PathVariable String username);
}*/
