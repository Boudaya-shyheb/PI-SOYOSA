apiVersion: v1
kind: Service
metadata:
  name: training-service
  namespace: pi-soyosa
spec:
  selector:
    app: training-service
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082
  type: ClusterIP
