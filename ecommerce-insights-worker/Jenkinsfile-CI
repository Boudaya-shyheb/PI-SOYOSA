pipeline {
    agent any

    environment {
        APP_NAME = "ecommerce-insights-worker"
        DOCKER_IMAGE = "oussema22/pi-ecommerce-insights-worker:latest"
        CD_JOB = "worker-CD"   // 👈 Jenkins CD job name
    }

    stages {

        stage('Checkout') {
            steps {
                cleanWs()
                retry(3) {
                    checkout scm
                }
            }
        }

        stage('Install Dependencies') {
            steps {
                dir('ecommerce-insights-worker') {
                    sh "npm install"
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                dir('ecommerce-insights-worker') {
                    sh "docker build -t ${DOCKER_IMAGE} ."
                }
            }
        }

        stage('Push Docker Image') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'docker-hub-creds', usernameVariable: 'USER', passwordVariable: 'PASS')]) {
                    sh """
                        echo $PASS | docker login -u $USER --password-stdin
                        docker push ${DOCKER_IMAGE}
                    """
                }
            }
        }

        stage('Trigger CD Pipeline') {
            steps {
                echo "🚀 Triggering CD pipeline..."
                build job: "${CD_JOB}", wait: false
            }
        }
    }

    post {
        success {
            echo "✅ CI finished successfully!"
        }

        failure {
            echo "❌ CI failed!"
        }

        always {
            cleanWs()
        }
    }
}
